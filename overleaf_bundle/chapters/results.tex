This chapter presents the main empirical results.  We first summarize predictive multiplicity and spatial structure across datasets (Section~\ref{sec:results-metrics}), then compare soft prediction variance with hard conflict ratio and present a quadrant analysis (Section~\ref{sec:results-conflict}), report null-hypothesis testing (Section~\ref{sec:results-null}), global vs.\ per-family comparisons (Section~\ref{sec:results-global-vs-family}), spatial pattern analysis (Section~\ref{sec:results-spatial}), sensitivity analyses (Sections~\ref{sec:results-sensitivity-K}--\ref{sec:results-sensitivity-kNN}), hyperparameter and family importance (Section~\ref{sec:results-hp}), calibration robustness (Section~\ref{sec:results-calibration}), synthetic validation (Section~\ref{sec:results-synthetic}), interpretable rules (Section~\ref{sec:results-rules}), and four additional robustness analyses: decision boundary proximity (Section~\ref{sec:results-margin}), fairness exposure (Section~\ref{sec:results-fairness}), alternative graph constructions (Section~\ref{sec:results-alt-graph}), and aggregated drivers across seeds (Section~\ref{sec:results-agg-drivers}).  All analyses use the global Rashomon set (top-$K$ with $K=25$) and default spatial parameters ($k=30$) unless stated otherwise.

%% ---------------------------------------------------------------------------
\subsection{Multiplicity and Spatial Metrics by Dataset}
\label{sec:results-metrics}

Table~\ref{tab:dataset_summary} summarizes mean predictive variance and global Moran's $I$ of pointwise variance, aggregated over 10 outer runs (mean $\pm$ standard deviation) for COMPAS, German Credit, and Adult.

Across datasets, predictive multiplicity is consistently present but varies substantially in magnitude and spatial organization. COMPAS exhibits the strongest spatial structure, with the highest Moran's $I$ ($0.20 \pm 0.08$), indicating pronounced clustering of predictive instability in feature space. German Credit shows moderate spatial structure (Moran's $I = 0.10 \pm 0.04$), but the highest overall multiplicity magnitude, with substantially larger predictive variance and conflict values. Adult displays lower multiplicity magnitude and weaker spatial clustering ($0.07 \pm 0.02$), though spatial dependence remains statistically significant across all runs.

The number of HH LISA hotspots further highlights structural differences across datasets. In absolute terms, Adult contains the largest number of hotspot points ($286.1$ on average), followed by COMPAS ($86.7$), while German Credit exhibits very few HH observations. However, absolute HH counts are not directly comparable across datasets due to differences in dataset size and the use of dataset-specific $k$ in the kNN graph. Interpreted alongside Moran's $I$, the results indicate that COMPAS exhibits the strongest spatial concentration of multiplicity, while Adult shows more diffuse but still structured hotspot regions.

\begin{table}[htbp]
\centering
\caption{Summary of multiplicity and spatial metrics by dataset (mean $\pm$ std over 10 outer runs, $K=25$, dataset-specific default $k$: 30 for COMPAS and German Credit, 60 for Adult).}
\label{tab:dataset_summary}
\input{presentation_assets/tab/dataset_summary.tex}
\end{table}

Additional multiplicity metrics computed across all runs include: mean ambiguity, disagreement rate ($\delta = 0.05$), discrepancy, and neighborhood agreement. German Credit exhibits the highest disagreement and discrepancy values, indicating greater diversity within the Rashomon set, while Adult shows lower disagreement and more stable predictions. COMPAS lies between these extremes but exhibits stronger spatial concentration of disagreement.

Figure~\ref{fig:dataset_comparison_bars} provides a compact comparison of key metrics across datasets, including Moran's $I$, HH hotspot count, mean predictive variance, and mean conflict ratio. COMPAS shows the strongest spatial structure, while German Credit exhibits the highest overall variance and conflict. Adult displays lower predictive variance and weaker spatial clustering than German Credit, but a substantially larger number of HH hotspot observations, indicating more spatially dispersed instability.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{dataset_comparison_bars.pdf}
\caption{Dataset comparison: Moran's $I$, HH count, mean variance, and mean conflict (mean $\pm$ std over 10 outer runs, $K=25$, dataset-specific $k$). COMPAS shows the strongest spatial structure, German Credit the highest multiplicity magnitude, and Adult more diffuse hotspot patterns.}
\label{fig:dataset_comparison_bars}
\end{figure}

Overall, these results establish that predictive multiplicity is strongly dataset-dependent. COMPAS exhibits highly localized and spatially concentrated instability, German Credit shows high overall predictive variability with weaker spatial organization, and Adult displays moderate but consistently structured multiplicity with more diffuse hotspot regions. In the following sections, we focus on COMPAS as a representative dataset for detailed spatial analysis, and return to cross-dataset comparisons where relevant.

%% ---------------------------------------------------------------------------
\subsection{Conflict Ratio vs Soft Prediction Variance}
\label{sec:results-conflict}

This thesis reports two complementary per-observation disagreement metrics (defined in Chapter~3): \emph{soft prediction variance}~$v_i$, which captures the spread of predicted probabilities across Rashomon models, and the \emph{conflict ratio}~$c_i$, which measures hard-label disagreement (the fraction of models on the minority side of the 0.5 decision threshold). Both are computed for each test observation on every run.

\subsubsection{Why Both Metrics Are Needed}

Soft variance and hard conflict capture distinct aspects of predictive multiplicity. An observation can have high variance without high conflict (e.g., probabilities spread across $[0.6, 0.9]$, where all models agree on the positive label) or high conflict without high variance (e.g., probabilities tightly clustered around 0.5, where small differences lead to label flips). Reporting both metrics is therefore necessary to capture both probability-level instability and decision-level disagreement.

\subsubsection{Dataset-Level Conflict Metrics}

Table~\ref{tab:conflict_summary} reports conflict-related metrics aggregated over 10 outer runs. Across datasets, conflict prevalence varies substantially. On COMPAS, 17.9\% of test points exhibit nonzero conflict ($c_i > 0$) and 5.9\% show severe disagreement ($c_i \geq 0.25$), accompanied by strong spatial clustering (conflict Moran's $I = 0.17$). German Credit exhibits the highest fraction of conflicted points (30.1\%) and the highest overall conflict magnitude, but substantially weaker spatial structure (Moran's $I = 0.05$), indicating that disagreement is more dispersed. Adult shows lower overall conflict levels (mean $c_i = 0.017$) and a smaller fraction of conflicted points, but still exhibits consistent spatial clustering of conflict across all runs.

\begin{table}[htbp]
\centering
\caption{Conflict metrics by dataset (mean $\pm$ std over 10 outer runs, global top-$K=25$, dataset-specific $k$).}
\label{tab:conflict_summary}
\small
\begin{tabular}{lccccc}
\hline
Dataset & Mean $c_i$ & $c_i > 0$ (\%) & $c_i \geq 0.25$ (\%) & Conflict Moran's $I$ & Conflict HH \\
\hline
COMPAS & $0.035 \pm 0.007$ & $17.9 \pm 2.9$ & $5.9 \pm 1.6$ & $0.17 \pm 0.03$ & $107.9 \pm 24.2$ \\
German & $0.059 \pm 0.022$ & $30.1 \pm 8.5$ & $9.9 \pm 4.6$ & $0.05 \pm 0.03$ & $0.5 \pm 0.8$ \\
Adult  & $0.017 \pm 0.001$ & $11.5 \pm 0.8$ & $3.2 \pm 0.5$ & $0.06 \pm 0.02$ & $190.3 \pm 30.1$ \\
\hline
\end{tabular}
\end{table}

These results highlight that high disagreement rates do not necessarily imply strong spatial structure: German Credit exhibits frequent conflict but weak clustering, whereas COMPAS shows both substantial disagreement and clear spatial concentration. Adult occupies an intermediate position, with lower disagreement levels than COMPAS and German Credit but still consistent positive spatial clustering across runs, albeit weaker than on COMPAS.

\subsubsection{Overlap Between Variance-Based and Conflict-Based Hotspots}

The Jaccard similarity between variance-based and conflict-based HH masks is low on COMPAS ($0.06 \pm 0.05$), indicating that the two metrics identify largely \emph{different} observations as hotspots. This is expected: variance-HH captures regions where predicted probabilities vary widely, while conflict-HH identifies regions where predictions cross the decision boundary. The two metrics therefore capture complementary notions of instability rather than redundant ones.

On German Credit, the mean Jaccard similarity is numerically higher ($0.21 \pm 0.42$), but this estimate is unstable and should be interpreted cautiously because both hotspot sets are often very small. On Adult, overlap remains limited ($0.10 \pm 0.05$), indicating that variance-based and conflict-based hotspots capture different aspects of predictive instability even in larger datasets with many hotspot observations.

\subsubsection{Quadrant Analysis}
\label{sec:results-quadrant}

To characterize the joint behavior of soft variance and hard conflict, we partition test observations into four quadrants using the 90th percentile of each metric within each run as thresholds. This focuses the analysis on the upper tail of each disagreement measure, i.e., observations with unusually large variance and/or conflict relative to the rest of the run:

\begin{itemize}
  \item \textbf{A (high variance, high conflict):} both soft and hard disagreement are elevated.
  \item \textbf{B (high variance, low conflict):} models differ in probability space but agree on the label.
  \item \textbf{C (low variance, high conflict):} predictions are tightly clustered near 0.5, leading to label flips despite low spread.
  \item \textbf{D (low variance, low conflict):} stable observations with minimal disagreement.
\end{itemize}

Table~\ref{tab:quadrant_compas} reports the quadrant breakdown for COMPAS (mean $\pm$ std over 10 seeds). The dominant regime is D (81.2\%), indicating that most observations are stable. However, the remaining points reveal distinct forms of instability: quadrant B (7.7\%) captures ``probability-unstable but label-stable'' observations, while quadrant C (8.7\%) identifies observations near the decision boundary where small differences lead to label changes. Quadrant A (2.3\%) represents the most critical cases, combining high probability spread with frequent label disagreement.

\begin{table}[htbp]
\centering
\caption{Quadrant breakdown for COMPAS (mean $\pm$ std over 10 outer runs, global top-$K=25$). Thresholds: 90th percentile of variance and conflict per run.}
\label{tab:quadrant_compas}
\small
\begin{tabular}{lcccc}
\hline
Quadrant & Count & Fraction & Mean $v_i$ & Mean $c_i$ \\
\hline
A (high var, high conflict) & $33.9 \pm 8.2$ & $0.023 \pm 0.006$ & 0.0068 & 0.290 \\
B (high var, low conflict)  & $111.6 \pm 8.1$ & $0.077 \pm 0.006$ & 0.0043 & 0.010 \\
C (low var, high conflict)  & $125.3 \pm 10.0$ & $0.087 \pm 0.007$ & 0.0011 & 0.278 \\
D (low var, low conflict)   & $1172.2 \pm 9.9$ & $0.812 \pm 0.007$ & 0.0009 & 0.005 \\
\hline
\end{tabular}
\end{table}

Overall, the quadrant analysis confirms that predictive multiplicity manifests in multiple forms, affecting both probability estimates and classification decisions. These distinctions are essential for interpreting downstream spatial patterns, as different types of disagreement may concentrate in different regions of the feature space.

%% ---------------------------------------------------------------------------
\subsection{Null Hypothesis Testing}
\label{sec:results-null}

To assess whether the observed spatial structure exceeds what would be expected by chance, we compare observed Moran's $I$ to a permutation-based null distribution (Section~\ref{sec:exp-null}). In this null model, each model's test-set predictions are independently permuted across observations ($R = 100$ permutations per run), preserving per-model marginal distributions while removing any spatial alignment between predictions and feature space.

Table~\ref{tab:null_significance} reports the fraction of runs with significant Moran's $I$ ($p < 0.05$). 

\begin{table}[htbp]
\centering
\caption{Fraction of runs with significant Moran's $I$ ($p < 0.05$) under the permutation null, by dataset.}
\label{tab:null_significance}
\input{presentation_assets/tab/null_significance.tex}
\end{table}

The results provide strong evidence that the observed spatial structure is not attributable to chance. All three datasets (COMPAS, German Credit, and Adult) exhibit significant spatial autocorrelation in all 10 runs. In every case, the empirical p-value attains the minimum possible value under $R=100$ permutations, namely $p = 1/101 \approx 0.0099$

These findings indicate that spatial clustering of predictive multiplicity is a consistent and robust property across datasets, rather than a dataset-specific artifact. While the strength of clustering differs (strongest on COMPAS, weaker on Adult), statistical significance is uniformly high.

Figure~\ref{fig:null_moran_example} illustrates this effect for a representative COMPAS run (seed 0), where the observed Moran's $I$ lies far outside the null distribution. This large separation indicates that the spatial clustering of predictive variance cannot be explained by random variation alone.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.6\textwidth]{null_moran_compas_seed0.pdf}
\caption{Null distribution of Moran's $I$ (100 permutations) vs.\ observed value for COMPAS seed 0. The observed value (vertical line) exceeds all null samples ($p < 0.01$).}
\label{fig:null_moran_example}
\end{figure}

\subsubsection{Conflict-Based Null Testing}

In addition to variance-based Moran's $I$, we repeat the null experiment using the pointwise conflict ratio $c_i$ (Section~\ref{sec:meth-conflict}) as the spatial variable. This tests whether hard-label disagreement---a complementary notion of multiplicity---also exhibits statistically significant spatial clustering.

The results mirror the variance-based findings. On COMPAS, conflict Moran's $I$ averages $0.17 \pm 0.03$ (compared to $0.20 \pm 0.08$ for variance), while German Credit and Adult show weaker but still positive clustering. Under the permutation null, conflict Moran's $I$ is significant ($p < 0.01$) in all runs for all datasets. 

This confirms that spatial clustering is not specific to the soft prediction variance metric, but reflects a broader phenomenon of localized disagreement that is also visible at the level of discrete classification decisions.

\subsubsection{Aggregated Null Summary}

Figure~\ref{fig:null_summary} summarizes the null experiment across all seeds. The left panel compares observed Moran's $I$ (boxplots) to the null mean $\pm$ standard deviation for both variance-based and conflict-based metrics. The right panel reports standardized z-scores, $z = (\text{observed} - \mu_{\text{null}}) / \sigma_{\text{null}}$.

Across all runs, COMPAS, German Credit, and Adult show large positive z-scores, consistent with the permutation-test results and indicating strong separation between the observed Moran's $I$ values and the null distribution. Differences across datasets are reflected in the magnitude of z-scores rather than their significance: COMPAS shows the strongest separation from the null, while Adult exhibits smaller but still clearly positive deviations.

Overall, these results demonstrate that the observed spatial clustering of predictive multiplicity is highly unlikely to arise under a spatially unstructured null model. This provides strong statistical support for treating multiplicity as a structured, feature-space-dependent phenomenon rather than random noise.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\textwidth]{null_summary_across_seeds.pdf}
\caption{Aggregated null summary. Left: observed Moran's $I$ (boxplots) vs.\ null mean $\pm$ std for both variance-based and conflict-based metrics per dataset. Right: z-scores for variance and conflict Moran's $I$ across seeds; dashed lines mark $z = \pm 1.96$.}
\label{fig:null_summary}
\end{figure}

%% ---------------------------------------------------------------------------
\subsection{Global vs Per-Family Rashomon Sets}
\label{sec:results-global-vs-family}

We compare the \textbf{global Rashomon set} (top-$K=25$ across all families; Section~\ref{sec:meth-rashomon}) with \textbf{per-family Rashomon sets} ($K$-each mode: top-$K=25$ within each model family, up to 125 models total). The global set pools near-optimal models across families; the per-family construction isolates disagreement within each model class.

Tables~\ref{tab:global_summary} and~\ref{tab:family_summary} report mean variance, Moran's $I$, mean HH counts, and the fraction of runs with significant Moran's $I$. Table~\ref{tab:global_summary} lists these quantities for the global set on German Credit, Adult, and COMPAS. Table~\ref{tab:family_summary} gives the per-family breakdown for COMPAS (aggregated over outer seeds).

\begin{table}[htbp]
\centering
\caption{Global Rashomon set (top-25 overall): multiplicity and spatial metrics by dataset (mean $\pm$ std over runs).}
\label{tab:global_summary}
\input{presentation_assets/tab/global_summary.tex}
\end{table}

\begin{table}[htbp]
\centering
\caption{Per-family Rashomon sets (top-25 per family; COMPAS): multiplicity and spatial metrics by model family (mean $\pm$ std over runs).}
\label{tab:family_summary}
\input{presentation_assets/tab/family_summary.tex}
\end{table}

\begin{figure}[htbp]
\centering
{\small\textbf{(a)} COMPAS}\\[-0.2em]
\includegraphics[width=0.92\textwidth]{family_vs_global_spatial_compas.pdf}\\[0.55em]
{\small\textbf{(b)} German Credit}\\[-0.2em]
\includegraphics[width=0.92\textwidth]{family_vs_global_spatial_german.pdf}\\[0.55em]
{\small\textbf{(c)} Adult}\\[-0.2em]
\includegraphics[width=0.92\textwidth]{family_vs_global_spatial_adult.pdf}
\caption{Per-family vs.\ global Rashomon sets (mean $\pm$ std over runs). Each row: mean HH count (left) and Moran's $I$ (right) for top-$K{=}25$ models within each family (bars), with the global top-$K{=}25$ mean as a horizontal dashed line. \textbf{(a)}~COMPAS---Table~\ref{tab:family_summary} gives the corresponding numerical values. \textbf{(b)}~German Credit---same layout with much smaller absolute HH counts (Table~\ref{tab:global_summary}). \textbf{(c)}~Adult---wide spread across families; MLP and RF attain the largest Moran's $I$ and HH counts among families, while logistic regression is comparatively low on both panels.}
\label{fig:hh_by_family}
\end{figure}

\paragraph{COMPAS (main case study).}
On COMPAS, Table~\ref{tab:family_summary} shows clear heterogeneity across families. MLP attains the strongest spatial clustering (Moran's $I = 0.422 \pm 0.049$) and the largest mean HH count ($208.5 \pm 39.8$), in each case well above the global-set averages in Table~\ref{tab:global_summary} ($0.199 \pm 0.077$ and $86.7$). kNN carries the highest mean variance ($0.0107 \pm 0.0014$) but the lowest Moran's $I$ among families ($0.090 \pm 0.027$), which is consistent with large disagreement that is not strongly concentrated in feature space. Logistic regression has the smallest mean variance ($0.00006 \pm 0.00013$) and, on average, fewer HH points than MLP ($57.7 \pm 77.4$ vs.\ $208.5 \pm 39.8$), though its Moran's $I$ estimate is more variable across seeds ($0.172 \pm 0.198$). Random forest and GBM fall between these extremes on Moran's $I$ and HH counts.

Figure~\ref{fig:hh_by_family}(a) mirrors Table~\ref{tab:family_summary}: several families lie above the global dashed line in one or both panels, with MLP the most pronounced example for HH count and Moran's $I$. Thus pooling into a single global Rashomon set can \emph{understate} the hotspot intensity and clustering visible within a single high-multiplicity family.

\paragraph{German Credit and Adult (supporting comparisons).}
Table~\ref{tab:global_summary} already separates the datasets in scale: global mean HH counts are very small on German Credit ($2.7$) compared with COMPAS ($86.7$) and Adult ($286.1$), while global Moran's $I$ is largest on COMPAS.

For German Credit, Figure~\ref{fig:hh_by_family}(b) uses the same bar-vs.-dashed layout as COMPAS. The relative ordering of families varies across the two metrics, so multiplicity cannot be reduced to a single family-independent spatial summary; however, the absolute magnitudes are modest, in line with the tiny global HH totals. Any parallel to COMPAS is therefore qualitative and scale-dependent, not a claim of identical effect sizes.

For Adult, Figure~\ref{fig:hh_by_family}(c) shows a wide spread across families on both panels. MLP and random forest stand out with the strongest Moran's $I$ and HH counts among families, whereas logistic regression sits toward the lower end of both metrics. The ordering and gaps are not a carbon copy of COMPAS; in particular, the families that stand out most strongly on Adult differ from those emphasized by the COMPAS per-family summary. Nonetheless, the figure supports the same substantive point: predictive multiplicity remains \emph{model-dependent}, and a globally pooled set compresses family-level structure.

Taken together, the three panels reinforce two conclusions. First, which families drive structured disagreement is not fixed across datasets. Second, aggregating across families can mask pronounced per-family spatial signals (most visibly for COMPAS and Adult, where per-family bars often exceed the global reference in at least one panel). This motivates the variance decomposition analysis in the following section.


%% ---------------------------------------------------------------------------
\subsection{Spatial Patterns and Hotspot Structure}
\label{sec:results-spatial}

This section examines the spatial organization of predictive multiplicity on COMPAS, focusing on the location, stability, and internal structure of HH hotspots. While previous sections establish the existence of spatial clustering, we now analyze how this structure manifests in feature space.

\subsubsection{HH Location and Frequency}

Figure~\ref{fig:hh_location} shows HH hotspots projected onto the first two principal components of COMPAS test features (seed 2, global top-$K = 25$). HH points (red) concentrate in a distinct region of feature space rather than being scattered uniformly, indicating that multiplicity is localized to specific subpopulations.

The stability analyses in this subsection use the \emph{fixed-test-set} splitting mode (Section~\ref{sec:exp-splits}), ensuring that the same observations are compared across runs.

Figure~\ref{fig:hh_stability_freq} shows the HH selection frequency across 10 seeds. Only 2.24\% of test points are classified as HH in $\geq 80$\% of runs, and 6.26\% in $\geq 50$\% of runs, indicating limited pointwise stability. However, the frequency map reveals that these unstable points consistently lie within the same region of feature space, suggesting that instability is structured at the region level rather than tied to specific observations.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.55\textwidth]{hh_location_compas.pdf}
\caption{HH hotspots (red) vs.\ other points (gray) in PCA space for COMPAS (representative run; selected by median HH count). HH points concentrate in a specific region rather than being scattered uniformly.}
\label{fig:hh_location}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.55\textwidth]{hh_stability_freq_compas.pdf}
\caption{HH selection frequency across 10 seeds for COMPAS. Color intensity encodes how often each point is classified as HH.}
\label{fig:hh_stability_freq}
\end{figure}

\subsubsection{Pairwise Overlap of HH Masks}

Figure~\ref{fig:hh_jaccard} shows the Jaccard similarity of HH masks between all pairs of seeds. The mean pairwise Jaccard is 0.025 (max 0.084), indicating that the exact set of HH points varies substantially across runs. 

Crucially, this low pointwise overlap does not imply instability of the spatial phenomenon itself. Combined with the frequency maps, it shows that predictive multiplicity is stable at the \emph{region level} but unstable at the \emph{point level}: the same areas of feature space repeatedly exhibit elevated disagreement, while the specific points classified as HH fluctuate across runs.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.5\textwidth]{hh_jaccard_heatmap_compas.pdf}
\caption{Jaccard similarity of HH masks across seed pairs for COMPAS. Low overlap reflects sensitivity at the point level.}
\label{fig:hh_jaccard}
\end{figure}

\subsubsection{Regional Structure of Hotspots}

Table~\ref{tab:hh_component_summary} summarizes HH connected-component statistics (minimum component size 5). On average, COMPAS exhibits $3.5$ connected components per run, with a mean maximum component size of $43.8$ points (median $34$).

These results indicate that multiplicity hotspots form a small number of coherent regions rather than many small fragments. In representative runs, a single dominant component typically contains the majority of HH points, with additional smaller components representing secondary regions of instability. This suggests that predictive multiplicity is regionally concentrated rather than uniformly distributed.

\begin{table}[htbp]
\centering
\caption{HH connected-component statistics across runs (min component size 5).}
\label{tab:hh_component_summary}
\input{presentation_assets/tab/hh_component_summary.tex}
\end{table}

\subsubsection{Spatial Scale of Clustering}

To assess the spatial scale of multiplicity, we examine how Moran's $I$ changes as the neighborhood size $k$ increases. Figure~\ref{fig:spatial_correlogram} shows an example spatial correlogram for COMPAS. Moran's $I$ is highest for small $k$ and decreases smoothly as $k$ increases, while remaining positive across the examined range.

This pattern suggests a multi-scale structure: strong local clustering of instability is embedded within broader regions of moderate multiplicity. Thus, predictive multiplicity is not confined to isolated neighborhoods but persists across multiple spatial scales. The corresponding aggregated sensitivity analysis across runs is reported in Section~\ref{sec:results-knn-sensitivity}.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.62\textwidth]{spatial_correlogram_compas.pdf}
\caption{Spatial correlogram for COMPAS: Moran's $I$ as a function of neighborhood size $k$ for an example run. Spatial autocorrelation is strongest at small neighborhood scales and decreases smoothly as $k$ increases, while remaining positive throughout.}
\label{fig:spatial_correlogram}
\end{figure}

\subsubsection{Alternative Local Statistics (Gi*)}

To verify that the observed hotspot structure is not specific to LISA, we apply the Getis--Ord Gi* statistic. While LISA identifies relative clustering, Gi* highlights regions of high absolute intensity.

Empirically, Gi* hotspots form a subset of LISA hotspots and exhibit substantially higher predictive variance. This suggests that multiplicity regions have internal structure, consisting of a high-intensity core surrounded by a broader region of moderate instability. The low overlap between LISA and Gi* masks suggests that these statistics emphasize different aspects of spatial multiplicity: LISA highlights relative local association, whereas Gi* concentrates more strongly on high-intensity cores.

\subsubsection{Moran's $I$ and HH Count Per Run}

Figure~\ref{fig:spatial_per_run} shows per-run variability in Moran's $I$ and HH count. Both metrics vary across seeds (Moran's $I$: 0.11--0.36; HH count: 21--160), reflecting the stochastic nature of the Rashomon set. However, all runs exhibit positive Moran's $I$, confirming that spatial clustering is a consistent property of the data rather than an artifact of specific splits.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{hh_moran_per_run_compas.pdf}
\caption{Moran's $I$ and HH count per run for COMPAS, showing variability across seeds.}
\label{fig:spatial_per_run}
\end{figure}

%% ---------------------------------------------------------------------------
\subsection{Sensitivity to Rashomon Set Size $K$}
\label{sec:results-sensitivity-K}

To assess how the choice of Rashomon set size affects the results, we vary $K \in \{5, 10, 15, 20, 25, 30, 35, 40, 45, 50\}$ using global top-$K$ selection (Section~\ref{sec:meth-rashomon}). Figure~\ref{fig:sensitivity_K} shows the sensitivity of both variance-based and conflict-based metrics to $K$, including mean variance, Moran's $I$, HH count, mean conflict, conflict Moran's $I$, and conflict-HH count.

Mean predictive variance increases monotonically with $K$ for all datasets, reflecting the inclusion of additional near-optimal models and thus greater prediction diversity. This trend is clearly visible for COMPAS, German Credit, and Adult, where variance grows steadily with $K$ (see Figure~\ref{fig:sensitivity_K} and corresponding dataset-specific plots).

In contrast, Moran's $I$ stabilizes beyond $K \approx 20$--25 for COMPAS (remaining around 0.18--0.21), and shows only gradual changes for German Credit and Adult. Similarly, the number of HH hotspots remains relatively stable for COMPAS ($\sim$77--91) and remains low for German Credit, while increasing more strongly for Adult due to its larger dataset size and more diffuse hotspot structure (see ).

Conflict-based metrics show a similar qualitative pattern: mean conflict increases with $K$, while conflict Moran's $I$ and conflict-HH counts stabilize once moderately sized Rashomon sets are reached. This indicates that both probability-level and label-level disagreement increase in magnitude but maintain similar spatial organization.

These results indicate that while the magnitude of disagreement increases with $K$, its \emph{spatial organization} is largely unaffected beyond moderate Rashomon set sizes. This supports the use of $K = 25$ as a default that captures the relevant structure of predictive multiplicity, while avoiding substantially larger Rashomon sets that primarily increase disagreement magnitude without materially changing its spatial structure.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\textwidth]{sensitivity_K_curves_compas.pdf}
\caption{Sensitivity to Rashomon set size $K$ on COMPAS (mean $\pm$ std over runs). Top row: mean variance, Moran's $I$ (variance), and variance-HH count. Bottom row: mean conflict, Moran's $I$ (conflict), and conflict-HH count.}
\label{fig:sensitivity_K}
\end{figure}

%% ---------------------------------------------------------------------------

\subsection{Sensitivity to kNN Neighborhood Size $k$}
\label{sec:results-sensitivity-kNN}

To evaluate the effect of the neighborhood definition in the feature-space graph (Section~\ref{sec:meth-graph}), we vary $k$ over a broad range and examine the resulting variance-based and conflict-based spatial metrics. Figure~\ref{fig:sensitivity_kNN} shows the COMPAS sensitivity curves, while the corresponding plots for German Credit and Adult are reported in the supplementary material.

As expected, mean predictive variance is invariant to $k$, since it does not depend on the graph structure. In contrast, Moran's $I$ decreases with increasing $k$ for all datasets, reflecting the smoothing effect of larger neighborhoods. On COMPAS, variance-based Moran's $I$ drops from roughly $0.29$ at small $k$ to about $0.12$ at $k=100$, while conflict Moran's $I$ shows a similar monotone decline. German Credit and Adult exhibit the same qualitative pattern, with positive Moran's $I$ values throughout but progressively weaker local autocorrelation as neighborhoods become larger. 

At the same time, the number of detected hotspots changes with $k$. On COMPAS and Adult, both variance-HH and conflict-HH counts increase with larger neighborhoods, whereas on German Credit the absolute counts remain very small and fluctuate only modestly. This reflects the fact that larger neighborhoods smooth the local signal and can increase the number of points classified as belonging to broader statistically significant clusters, even as the strength of local autocorrelation decreases. 

Importantly, despite these changes in magnitude, spatial clustering remains consistently positive across all examined values of $k$. This indicates that the presence of spatial structure is robust to the choice of neighborhood size, even though the exact extent of detected hotspot regions depends on $k$.

For moderate neighborhood sizes, the curves begin to flatten, supporting the use of dataset-specific default values ($k=30$ for COMPAS and German Credit, $k=60$ for Adult) as reasonable compromises between locality and graph stability. Together with the spatial correlogram analysis in Section~\ref{sec:results-spatial}, these results show that predictive multiplicity exhibits a stable multi-scale structure rather than being an artifact of a particular neighborhood choice.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\textwidth]{sensitivity_kNN_curves_compas.pdf}
\caption{Sensitivity to kNN neighborhood size $k$ on COMPAS (mean $\pm$ std over runs). Top row: mean variance, Moran's $I$ (variance), and variance-HH count. Bottom row: Moran's $I$ (conflict) and conflict-HH count. Mean predictive variance remains flat, whereas both variance-based and conflict-based Moran's $I$ decrease with increasing $k$.}
\label{fig:sensitivity_kNN}
\end{figure}

%% ---------------------------------------------------------------------------
\subsection{Hyperparameter and Family Importance}
\label{sec:results-hp}

To identify which modeling choices drive predictive multiplicity, we apply the variance decomposition described in Section~\ref{sec:exp-hp-decomp}, using the per-family Rashomon set (top-25 per family, up to 125 models total). Each model's disagreement contribution $V_m$ (Eq.~\ref{eq:Vm}) is decomposed by hyperparameter; HP importance $\rho(h)$ (Eq.~\ref{eq:hp-importance}) measures the fraction of variance explained by that hyperparameter.

We first present a detailed case study on COMPAS (seed 0), followed by aggregated results across all seeds in Section~\ref{sec:results-agg-drivers}.

\subsubsection{Family Importance}

Model family (GBM, LogReg, MLP, RF, kNN) explains a substantial portion of predictive multiplicity. On COMPAS, family choice accounts for approximately 41\% of total prediction variance ($0.41 \pm 0.03$), increasing to 52\% within HH hotspots ($0.52 \pm 0.04$). This indicates that the relative importance of model class is even stronger in regions of high disagreement.

Across datasets, family importance is consistently high, reaching $0.62 \pm 0.05$ on Breast Cancer and $0.53 \pm 0.04$ on German Credit. In all cases, the contribution increases within HH regions, suggesting that multiplicity hotspots are particularly sensitive to model-family differences.

Table~\ref{tab:family_importance} provides a detailed breakdown for COMPAS seed 0, and Figure~\ref{fig:family_importance_by_subset} illustrates the difference between global, HH-only, and non-HH subsets.

\begin{table}[htbp]
\centering
\caption{Family importance (between-family variance / total variance) on COMPAS seed 0, by observation subset.}
\label{tab:family_importance}
\input{presentation_assets/tab/family_importance.tex}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.65\textwidth]{family_importance_by_subset_compas_seed0.pdf}
\caption{Family importance (ratio of between-family to total variance) on all test points, HH hotspots, and non-HH points for COMPAS seed 0.}
\label{fig:family_importance_by_subset}
\end{figure}

\subsubsection{Within-Family Hyperparameter Importance}

Within each model family, the variance decomposition identifies which hyperparameters drive disagreement. Table~\ref{tab:hp_importance} summarizes the most influential hyperparameters per family.

Regularization strength dominates for LogReg ($C$, 87\%) and kNN (number of neighbors, 86\%), indicating that model complexity controls are the primary drivers of variability in these families. For tree-based models, structural parameters such as depth are most important (RF: 67\%), while GBM is primarily influenced by learning rate (34\%). 

For MLP, multiple hyperparameters contribute, including weight decay $\alpha$ (25\%), learning rate (19\%), and architecture (14\%), indicating a more distributed source of variability.

\begin{table}[htbp]
\centering
\caption{Top hyperparameters per model family by variance decomposition of $V_m$ (COMPAS seed 0). $\rho(h)$ denotes HP importance.}
\label{tab:hp_importance}
\small
\begin{tabular}{llcc}
\hline
Family & Hyperparameter & $\rho(h)$ & Groups \\
\hline
LogReg & C & 0.865 & 22 \\
kNN & n\_neighbors & 0.855 & 6 \\
RF & max\_depth & 0.667 & 5 \\
GBM & learning\_rate & 0.337 & 5 \\
GBM & max\_depth & 0.258 & 3 \\
MLP & alpha & 0.250 & 20 \\
MLP & learning\_rate\_init & 0.189 & 20 \\
MLP & hidden\_layer\_sizes & 0.140 & 15 \\
\hline
\end{tabular}
\end{table}

\subsubsection{Hotspot-Specific Drivers}

When restricting the analysis to HH observations, the importance of hyperparameters changes. Figure~\ref{fig:hh_delta_hp} shows the difference in HP importance between HH regions and the full dataset.

On COMPAS, the importance of MLP learning rate initialization increases by approximately $+0.15$ within hotspots, indicating that optimization dynamics play a particularly strong role in high-multiplicity regions. In contrast, GBM hyperparameters such as learning rate and depth decrease slightly in importance, suggesting that disagreement in GBM is more uniformly distributed across the dataset.

This shift highlights that the drivers of multiplicity are not uniform: different modeling choices become more or less influential depending on the region of feature space.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.65\textwidth]{hh_delta_hp_importance_compas_seed0_MLP.pdf}
\caption{Change in HP importance ($\Delta\rho(h)$ = HH $-$ all) for MLP on COMPAS seed 0.}
\label{fig:hh_delta_hp}
\end{figure}

\subsubsection{Predictive Variance by HP Value}

Figure~\ref{fig:variance_by_hp_mlp} shows the distribution of per-model disagreement contribution $V_m$ across different hyperparameter values for MLP. Certain configurations systematically produce higher disagreement, indicating that multiplicity is not only driven by model class but also by specific parameter choices within each family.

This provides a more fine-grained view of multiplicity: while family choice explains a large portion of variance, hyperparameters determine how disagreement manifests within each model class.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.8\textwidth]{variance_by_hp_compas_seed0_MLP.pdf}
\caption{Per-model disagreement contribution $V_m$ by hyperparameter value for MLP on COMPAS seed 0.}
\label{fig:variance_by_hp_mlp}
\end{figure}

%% ---------------------------------------------------------------------------
\subsection{Calibration Robustness}
\label{sec:results-calibration}

To assess whether the observed spatial multiplicity is driven by probability miscalibration, we apply Platt scaling as described in Section~\ref{sec:exp-calibration}. A logistic regression model is fitted on validation-set predictions for each model and then applied to test-set predictions. Multiplicity metrics (including both variance and conflict ratio) are recomputed after calibration. Table~\ref{tab:calibration_summary} summarizes the resulting changes.

\begin{table}[htbp]
\centering
\caption{Calibration robustness: change in key metrics after Platt scaling (mean $\pm$ std over runs).}
\label{tab:calibration_summary}
\input{presentation_assets/tab/calibration_summary.tex}
\end{table}

Calibration has only a limited effect on spatial multiplicity. On average, Moran's $I$ decreases slightly ($\Delta \approx -0.02$), indicating that a small fraction of spatial structure may be attributable to miscalibration. However, the overall clustering pattern remains clearly present.

The Jaccard overlap between HH masks before and after calibration is moderate ($0.47 \pm 0.14$ on COMPAS, $0.42 \pm 0.37$ on German Credit, and $0.78 \pm 0.37$ on Breast Cancer), showing that hotspot regions shift but do not disappear. Brier score changes are negligible ($< 0.001$ on average), indicating that calibration does not materially affect predictive performance.

Overall, these results demonstrate that spatial multiplicity is predominantly structural. While calibration slightly alters the magnitude and exact location of hotspots, it does not eliminate the underlying spatial patterns, confirming that multiplicity reflects genuine model disagreement rather than artifacts of probability miscalibration.

%% ---------------------------------------------------------------------------
\subsection{Synthetic Data Validation}
\label{sec:results-synthetic}

To validate the proposed pipeline, we evaluate it on synthetic datasets with known ground-truth ambiguous regions. These experiments test whether LISA-based hotspot detection can recover regions of inherent predictive uncertainty.

\subsubsection{Single-Island Design}

The first dataset consists of two well-separated Gaussian clusters (stable regions) and one ambiguous island, generated as a uniform disk with $P(y=1|x) \approx 0.5$. The pipeline is applied using the same model families and top-$K=25$ selection.

\begin{itemize}
\item \textbf{Moran's $I$:} 0.459 (highly significant; null mean $-0.001 \pm 0.008$, $p < 0.01$), indicating strong spatial clustering of multiplicity.
\item \textbf{HH recovery:} 101 HH points detected; precision = 0.99, recall = 0.91, Jaccard = 0.90 relative to the ground-truth island (110 points).
\item \textbf{FDR sensitivity:} Results remain stable across $\alpha \in \{0.01, 0.05, 0.10, 0.20\}$, with Jaccard ranging from 0.87 to 0.93.
\end{itemize}

These results demonstrate that the method can accurately recover a single coherent region of ambiguity with both high precision and high recall.

\subsubsection{Three-Islands Design}

A more complex dataset is constructed with two stable clusters, three ambiguous islands, and additional outliers.

\begin{itemize}
\item \textbf{HH recovery:} At $\alpha = 0.05$, precision = 0.96, recall = 0.54, and Jaccard = 0.52. Only 2 of the 3 islands are recovered as connected components.
\item \textbf{FDR sensitivity:} Recall increases with $\alpha$ (from 0.42 at $\alpha=0.01$ to 0.64 at $\alpha=0.20$), while precision remains consistently above 0.94.
\end{itemize}

Compared to the single-island case, the recovery is less complete, reflecting the increased difficulty of detecting multiple smaller regions, particularly when local density is low. Some ambiguous regions fail to form sufficiently strong local clusters to be detected as HH components.

Overall, the synthetic experiments confirm that the proposed pipeline reliably identifies regions of predictive ambiguity when they are sufficiently large and coherent. Detection performance degrades gracefully for smaller or more fragmented regions, highlighting the role of spatial density in hotspot identification.

%% ---------------------------------------------------------------------------
\subsection{Interpretable Rules for High-Multiplicity Regions}
\label{sec:results-rules}

To make multiplicity hotspots actionable, we extract interpretable rules that describe HH regions and high-variance (HV, top 10\% by pointwise variance) observations in terms of original features. This provides a human-understandable characterization of which subpopulations are most affected by predictive multiplicity.

\subsubsection{Rule Extraction Methods}

We apply four complementary rule extraction approaches: (1) shallow decision trees, (2) L1-regularized logistic regression, (3) cluster-describe (DBSCAN followed by rule fitting), and (4) beam search for conjunctive rules maximizing lift. Rules are evaluated using support (coverage), purity ($P(\text{label}=1 \mid \text{rule region})$), and lift (purity relative to the base rate).

\subsubsection{Results on COMPAS}

On COMPAS (seed 0), the base rate is 10.2\% for HV (147 out of 1443 test points) and 3.3\% for HH (48 points). Across methods, the extracted rules consistently identify a small set of features that characterize high-multiplicity regions.

Decision tree rules achieve high purity (up to 1.0, lift $\approx 9.8$) for small but well-defined regions, indicating that certain subpopulations exhibit highly concentrated instability. L1-regularized logistic regression highlights prior offence count and race (``Other'' category) as key drivers of HV regions, suggesting that multiplicity is associated with specific demographic and behavioral characteristics. 

Beam search produces rules with strong out-of-bag (OOB) performance (purity $\sim$0.40, lift $\sim$12.0), again emphasizing prior offence count and age. Across methods, the consistency of selected features indicates that hotspot regions are not arbitrary but correspond to meaningful subgroups in the data. OOB bootstrap evaluation further confirms rule stability, with median purity remaining within 10\% of in-sample estimates.

Figure~\ref{fig:pca_hv_hh} shows HV and HH points in PCA space for COMPAS, illustrating that these regions form a coherent cluster in feature space. This aligns with earlier spatial analyses, reinforcing that multiplicity is concentrated in identifiable regions rather than scattered randomly.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.55\textwidth]{pca_hv_hh_compas.pdf}
\caption{HV (top 10\% variance) and HH (LISA hotspot) points in PCA space for COMPAS seed 0. Both sets overlap substantially and occupy a distinct region.}
\label{fig:pca_hv_hh}
\end{figure}

\subsubsection{Cross-Dataset Comparison}

Similar patterns are observed on other datasets, though with weaker structure. On German Credit, HV rules achieve moderate purity (0.3--0.5) and are driven by features such as credit amount and employment duration, indicating economically meaningful sources of disagreement. On Breast Cancer, HV regions correspond to intermediate cell measurements, reflecting inherent ambiguity near the classification boundary.

Overall, these results demonstrate that multiplicity hotspots can be described using simple, interpretable feature conditions. This shows that predictive multiplicity is not only spatially structured but also semantically meaningful, affecting identifiable subpopulations rather than arbitrary subsets of the data.


%% ---------------------------------------------------------------------------
\subsection{Decision Boundary Proximity}
\label{sec:results-margin}

A natural hypothesis is that high-variance observations arise primarily near the decision boundary, where the ensemble mean prediction $\bar{p}_i$ is close to 0.5. We test this by computing the margin $m_i = |\bar{p}_i - 0.5|$ and correlating it with pointwise variance.

Table~\ref{tab:margin_summary} summarizes the results. On Breast Cancer and German Credit, variance and margin are strongly \emph{negatively} correlated (Pearson $r = -0.85$ and $-0.56$, respectively), confirming that high-variance points tend to lie near the decision boundary. This indicates that, on these datasets, predictive multiplicity is largely driven by boundary ambiguity.

In contrast, on COMPAS the correlation is close to zero ($r = -0.04 \pm 0.09$), indicating that multiplicity is \emph{not} explained by proximity to the decision boundary. HH points exhibit similar margin values to non-HH points, suggesting that disagreement arises from structural differences between models rather than inherent classification uncertainty.

\begin{table}[htbp]
\centering
\caption{Variance vs.\ margin correlation (mean $\pm$ std across 10 seeds). Margin = $|\bar{p} - 0.5|$.}
\label{tab:margin_summary}
\small
\begin{tabular}{lcccc}
\hline
Dataset & Pearson $r$ & Spearman $\rho$ & Margin (HH) & Margin (non-HH) \\
\hline
Breast Cancer & $-0.85 \pm 0.14$ & $-0.99 \pm 0.01$ & $0.38 \pm 0.06$ & $0.47 \pm 0.01$ \\
COMPAS        & $-0.04 \pm 0.09$ & $-0.07 \pm 0.18$ & $0.19 \pm 0.04$ & $0.18 \pm 0.01$ \\
German        & $-0.56 \pm 0.13$ & $-0.70 \pm 0.10$ & $0.16 \pm 0.06$ & $0.26 \pm 0.02$ \\
\hline
\end{tabular}
\end{table}

This distinction is further supported by a Mann--Whitney U test (alternative: HH has lower margin than non-HH). The test is significant for all Breast Cancer runs ($p < 0.001$, fraction significant = 1.0) and for roughly half of German runs (fraction significant = 0.57), but only for 30\% of COMPAS runs. 

Figure~\ref{fig:variance_vs_margin} illustrates this difference: on COMPAS (seed 0), HH (red) and HV (orange) points are distributed across the full range of margins, rather than concentrating near the boundary. 

Taken together, these results show that predictive multiplicity can arise from two distinct mechanisms: boundary uncertainty (as in Breast Cancer and German Credit) and structural model disagreement (as in COMPAS). This reinforces the interpretation of COMPAS multiplicity as a structured, model-driven phenomenon rather than a simple consequence of classification ambiguity.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.65\textwidth]{variance_vs_margin_compas.pdf}
\caption{Pointwise variance vs.\ margin ($|\bar{p}-0.5|$) for COMPAS seed 0. HH and HV points span a wide range of margins, showing that multiplicity is not purely a boundary effect.}
\label{fig:variance_vs_margin}
\end{figure}

%% ---------------------------------------------------------------------------
\subsection{Fairness and Subgroup Exposure (COMPAS)}
\label{sec:results-fairness}

We examine whether HH and HV regions disproportionately affect sensitive groups on COMPAS (race and sex). For each seed, we compute the fraction of each group's test points that fall in HH or HV regions.

Table~\ref{tab:fairness} reports group-wise HH and HV rates (mean $\pm$ std across 10 seeds). African-Americans have a mean HH rate of 7.4\%, compared to 2.9\% for Caucasians and 1.9\% for Hispanics. The small-$n$ groups (Asian, Native American) show high rates but with large variance due to very few test points. HV rates follow a similar pattern (10.3\% for African-Americans vs.\ 7.3\% for Caucasians).

\begin{table}[htbp]
\centering
\caption{COMPAS: HH and HV rates by group (mean $\pm$ std across 10 seeds). Larger groups are more reliable.}
\label{tab:fairness}
\small
\begin{tabular}{llcccc}
\hline
Attribute & Group & $n$ (mean) & HH rate & HV rate & Mean variance \\
\hline
\multirow{6}{*}{Race}
& African-American & 729 & $0.074 \pm 0.054$ & $0.103 \pm 0.016$ & $0.0013 \pm 0.0004$ \\
& Caucasian        & 505 & $0.029 \pm 0.014$ & $0.073 \pm 0.013$ & $0.0012 \pm 0.0002$ \\
& Hispanic         & 128 & $0.019 \pm 0.023$ & $0.091 \pm 0.032$ & $0.0012 \pm 0.0003$ \\
& Other            &  71 & $0.113 \pm 0.124$ & $0.210 \pm 0.089$ & $0.0019 \pm 0.0005$ \\
\hline
\multirow{2}{*}{Sex}
& Female & 279 & $0.054 \pm 0.059$ & $0.127 \pm 0.030$ & $0.0014 \pm 0.0003$ \\
& Male   & 1164 & $0.056 \pm 0.032$ & $0.095 \pm 0.007$ & $0.0013 \pm 0.0003$ \\
\hline
\end{tabular}
\end{table}

A permutation test (1000 permutations of group labels) yields a mean $p$-value of 0.25 for race and 0.26 for sex, with significant disparity in 30\% (race) and 40\% (sex) of seeds. The observed disparity in HH rates across racial groups is moderately larger than expected under the null ($\Delta = 0.32$ observed vs.\ $0.13$ null mean for race), but not consistently significant across all seeds. This suggests that the racial composition of HH regions varies with the random split and Rashomon realization.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.7\textwidth]{fairness_hh_rate_by_race_compas.pdf}
\caption{HH rate by race on COMPAS (mean $\pm$ std across 10 seeds). African-Americans have a higher HH rate than Caucasians and Hispanics.}
\label{fig:fairness_hh_race}
\end{figure}

%% ---------------------------------------------------------------------------
\subsection{Alternative kNN Graph Constructions}
\label{sec:results-alt-graph}

To verify that the observed spatial patterns are not artifacts of a specific distance metric or feature representation, we compare alternative kNN graph constructions: (1) kNN in PCA-reduced space (15 components) and (2) kNN using cosine distance. Table~\ref{tab:alt_graph} summarizes Moran's $I$ and HH count for each method.

\begin{table}[htbp]
\centering
\caption{Moran's $I$ and HH count under alternative kNN graph constructions (mean $\pm$ std across 10 seeds, $k=30$).}
\label{tab:alt_graph}
\small
\begin{tabular}{llcc}
\hline
Dataset & Method & Moran's $I$ & HH count \\
\hline
\multirow{3}{*}{COMPAS}
& Euclidean (baseline)  & $0.199 \pm 0.077$ & $80.7 \pm 48.7$ \\
& PCA (15 comp.)        & $0.198 \pm 0.076$ & $82.2 \pm 50.1$ \\
& Cosine                & $0.213 \pm 0.083$ & $111.3 \pm 49.0$ \\
\hline
\multirow{3}{*}{German}
& Euclidean (baseline)  & $0.104 \pm 0.040$ & $2.8 \pm 2.1$ \\
& PCA (15 comp.)        & $0.098 \pm 0.040$ & $3.0 \pm 2.2$ \\
& Cosine                & $0.114 \pm 0.043$ & $5.8 \pm 4.7$ \\
\hline
\multirow{3}{*}{Breast Cancer}
& Euclidean (baseline)  & $0.032 \pm 0.021$ & $1.5 \pm 3.0$ \\
& PCA (15 comp.)        & $0.033 \pm 0.022$ & $1.8 \pm 3.4$ \\
& Cosine                & $0.037 \pm 0.023$ & $0.7 \pm 1.1$ \\
\hline
\end{tabular}
\end{table}

Across all datasets, Moran's $I$ remains highly consistent under different graph constructions. Euclidean and PCA-based graphs produce nearly identical values (differences $< 0.01$), indicating that dimensionality reduction does not materially affect the detected spatial structure. Cosine-based graphs yield slightly higher Moran's $I$ and HH counts for COMPAS and German Credit, suggesting that alternative similarity measures may emphasize different aspects of local structure, but without altering the overall pattern.

Figure~\ref{fig:alt_graph} illustrates these results visually. While the absolute number of detected hotspots varies somewhat across graph constructions, the presence and relative strength of spatial clustering remain stable.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{alternative_knn_comparison.pdf}
\caption{Moran's $I$ (left) and HH count (right) under three kNN graph constructions: Euclidean (baseline), PCA-reduced (15 components), and cosine distance.}
\label{fig:alt_graph}
\end{figure}

Overall, these findings demonstrate that the spatial clustering of predictive multiplicity is robust to the choice of distance metric and feature representation. The consistency across graph constructions indicates that the observed patterns reflect intrinsic structure in the data rather than artifacts of a particular similarity definition.

%% ---------------------------------------------------------------------------
\subsection{Aggregated Drivers Across Seeds}
\label{sec:results-agg-drivers}

To ensure that driver and interpretability claims are robust, we aggregate the variance decomposition results across all 10 seeds.

\subsubsection{Family Importance Across Seeds}

Table~\ref{tab:fam_imp_agg} and Figure~\ref{fig:fam_imp_agg} report family importance (ratio of between-family to total variance) aggregated over 10 seeds for each dataset. Key findings:

\begin{itemize}
\item Family importance is highest on Breast Cancer ($0.62 \pm 0.05$), followed by German ($0.53 \pm 0.04$) and COMPAS ($0.41 \pm 0.03$). This reflects that on Breast Cancer, model family choice (rather than within-family hyperparameters) is the dominant driver of disagreement.
\item Inside HH hotspots, family importance rises for all datasets: COMPAS $0.52 \pm 0.04$, German $0.57 \pm 0.08$, Breast Cancer $0.69 \pm 0.05$.
\end{itemize}

\begin{table}[htbp]
\centering
\caption{Family importance aggregated across 10 seeds: ratio of between-family to total variance.}
\label{tab:fam_imp_agg}
\small
\begin{tabular}{lcccc}
\hline
Dataset & All points & HH only & Seeds (all) & Seeds (HH) \\
\hline
Breast Cancer & $0.62 \pm 0.05$ & $0.69 \pm 0.05$ & 10 & 4 \\
COMPAS        & $0.41 \pm 0.03$ & $0.52 \pm 0.04$ & 10 & 10 \\
German        & $0.53 \pm 0.04$ & $0.57 \pm 0.08$ & 10 & 4 \\
\hline
\end{tabular}
\end{table}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{family_importance_aggregated.pdf}
\caption{Family importance (all test points vs.\ HH only), aggregated across 10 seeds with error bars.}
\label{fig:fam_imp_agg}
\end{figure}

\subsubsection{Hyperparameter Importance Across Seeds}

Figure~\ref{fig:hp_imp_agg} shows the top hyperparameters per family on COMPAS, aggregated across 10 seeds.  The ranking is highly consistent: LogReg regularization strength $C$ ($\rho = 0.94 \pm 0.05$), kNN number of neighbors ($0.79 \pm 0.17$), RF maximum depth ($0.64 \pm 0.07$), GBM learning rate ($0.31 \pm 0.05$), and MLP learning rate initialization ($0.22 \pm 0.10$) are the top drivers within their respective families in every seed.  These findings are consistent across German and Breast Cancer, with LogReg $C$ dominating across all datasets ($\rho > 0.93$).

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\textwidth]{hp_importance_aggregated_compas.pdf}
\caption{Top within-family HP importance on COMPAS (mean $\pm$ std across 10 seeds). Rankings are consistent across seeds: regularization strength and complexity parameters dominate.}
\label{fig:hp_imp_agg}
\end{figure}

\subsubsection{Rule Feature Frequency Across Seeds}

The interpretable rules extracted for COMPAS (Section~\ref{sec:results-rules}) consistently use the same features across seeds and rule extraction methods.  Table~\ref{tab:rule_freq} reports the frequency of feature appearance across all extracted rules.  Age and prior offence count dominate, appearing in 78 and 58 rules respectively.  Race-related features and sex appear less frequently but contribute to specific rules.

\begin{table}[htbp]
\centering
\caption{Feature frequency in extracted rules for COMPAS (across all methods and seeds).}
\label{tab:rule_freq}
\small
\begin{tabular}{lc}
\hline
Feature & Frequency \\
\hline
Age                         & 78 \\
Prior offence count         & 58 \\
Race (Other)                & 10 \\
Sex (Female)                & 8 \\
Charge degree (Misdemeanor) & 4 \\
\hline
\end{tabular}
\end{table}

%% ---------------------------------------------------------------------------
\subsection{Summary of Results}

The experimental results demonstrate that predictive multiplicity is a structured and robust phenomenon, rather than a purely local or random effect. Across datasets, models, and analysis choices, several consistent patterns emerge.

\begin{enumerate}

\item \textbf{Spatial structure:} Predictive multiplicity is spatially clustered in feature space. Global Moran's $I$ is positive and statistically significant under the permutation null for the vast majority of runs (100\% for COMPAS and German, 70\% for Breast Cancer). This confirms that disagreement is not randomly distributed but concentrates in specific regions.

\item \textbf{Multi-scale and region-level behavior:} Spatial clustering persists across neighborhood scales and forms coherent hotspot regions. While individual HH points vary across runs (Jaccard $\sim$0.025), the same regions of feature space repeatedly exhibit elevated multiplicity. This indicates region-level stability despite point-level variability.

\item \textbf{Complementary disagreement mechanisms:} Soft prediction variance and hard conflict ratio capture distinct aspects of multiplicity. Their low overlap (Jaccard $\approx 0.06$ on COMPAS) and quadrant analysis show that disagreement can occur either through probability spread or label instability, reinforcing the need for both metrics.

\item \textbf{Model-driven multiplicity:} Model family is a primary driver of predictive variability, explaining 41--62\% of variance globally and up to 69\% within hotspots. Different families induce distinct spatial patterns, and these differences are amplified in high-multiplicity regions.

\item \textbf{Hyperparameter effects:} Within families, a small set of hyperparameters consistently drives disagreement. Regularization and complexity parameters dominate (e.g., LogReg $C$, kNN neighbors), while neural networks exhibit more distributed sensitivity. These patterns are stable across seeds.

\item \textbf{Robustness to modeling choices:} The observed spatial structure is robust to key design choices. Varying Rashomon set size $K$ affects the magnitude of disagreement but not its spatial organization, while varying the neighborhood size $k$ changes the extent of detected regions but preserves clustering. Calibration and alternative graph constructions similarly leave the core structure intact.

\item \textbf{Validation on synthetic data:} On datasets with known ambiguous regions, the method recovers ground-truth instability with high precision (0.99) and recall (0.91), demonstrating that detected hotspots correspond to genuine sources of predictive uncertainty.

\item \textbf{Interpretability of hotspots:} High-multiplicity regions can be described by compact and stable rules, with lift up to 12$\times$ the base rate. These rules consistently involve a small set of features (e.g., prior offence count and age), indicating that multiplicity affects identifiable subpopulations.

\item \textbf{Multiple sources of multiplicity:} Predictive multiplicity arises from different mechanisms across datasets. On COMPAS, it is largely independent of decision-boundary proximity ($r \approx -0.04$), indicating structural model disagreement. On Breast Cancer and German Credit, multiplicity is more strongly associated with boundary uncertainty.

\item \textbf{Fairness implications:} Subgroup exposure analysis shows that certain groups (e.g., African-Americans on COMPAS) experience higher rates of multiplicity hotspots. However, these disparities are not consistently statistically significant across runs, indicating sensitivity to data splits and model selection.

\item \textbf{Consistency across seeds:} Driver importance, rule features, and spatial patterns remain stable across seeds, indicating that the observed multiplicity structure is not an artifact of specific training realizations.

\end{enumerate}

Overall, these findings show that predictive multiplicity is not merely a global nuisance metric, but a structured phenomenon with spatial, model-dependent, and interpretable characteristics. It concentrates in identifiable regions of feature space, is driven by specific modeling choices, and remains robust across analytical variations.
